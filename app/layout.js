import './globals.css';

export const metadata = {
  title: 'VEXTA | Digital Product Passport',
  description: 'A verifiable digital product passport for textile products.',
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}